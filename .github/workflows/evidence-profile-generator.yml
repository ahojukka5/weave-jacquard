name: Generate evidence profile slice

on:
  push:
    branches:
      - agent/evidence-profile-generator

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/evidence-profile-generator
          fetch-depth: 0

      - name: Generate implementation
        run: |
          python - <<'PY'
          from __future__ import annotations

          from pathlib import Path

          root = Path.cwd()

          def replace_once(path: Path, old: str, new: str) -> None:
              text = path.read_text(encoding="utf-8")
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{path}: expected one match, found {count}: {old[:80]!r}")
              path.write_text(text.replace(old, new), encoding="utf-8")

          evidence = root / "src/weave_frontend/compiler/evidence.py"
          evidence.write_text(
              '''"""Compiler evidence profile validation and capability requirements."""

from __future__ import annotations

from typing import Final

from ..errors import ValidationError

DEFAULT_EVIDENCE_PROFILE: Final = "none"
EVIDENCE_PROFILES: Final = ("none", "minimal", "full")
_EVIDENCE_PROTOCOLS: Final = {
    "none": (),
    "minimal": (
        "weavec-build-manifest-v1",
        "weavec-diagnostics-v1",
        "weavec-compilation-trace-v1",
    ),
    "full": (
        "weavec-build-manifest-v1",
        "weavec-diagnostics-v1",
        "weavec-compilation-trace-v1",
    ),
}


def normalize_evidence_profile(value: str | None) -> str:
    """Return one canonical evidence profile or reject the request."""

    profile = DEFAULT_EVIDENCE_PROFILE if value is None else value
    if not isinstance(profile, str) or profile not in EVIDENCE_PROFILES:
        admitted = ", ".join(EVIDENCE_PROFILES)
        raise ValidationError(
            "INVALID_EVIDENCE_PROFILE",
            f"evidence_profile must be one of: {admitted}",
        )
    return profile


def required_evidence_protocols(profile: str | None) -> tuple[str, ...]:
    """Return compiler protocols required before one profile may build."""

    normalized = normalize_evidence_profile(profile)
    return _EVIDENCE_PROTOCOLS[normalized]


__all__ = [
    "DEFAULT_EVIDENCE_PROFILE",
    "EVIDENCE_PROFILES",
    "normalize_evidence_profile",
]
''',
              encoding="utf-8",
          )

          init = root / "src/weave_frontend/compiler/__init__.py"
          replace_once(
              init,
              '''from .diagnostics import (
    BUILD_DIAGNOSTICS_FORMAT,
    COMPILER_DIAGNOSTICS_FORMAT,
    collect_build_diagnostics,
)
from .inputs import CompilerInputMixin, MaterializedSource, RenderedSource
''',
              '''from .diagnostics import (
    BUILD_DIAGNOSTICS_FORMAT,
    COMPILER_DIAGNOSTICS_FORMAT,
    collect_build_diagnostics,
)
from .evidence import (
    DEFAULT_EVIDENCE_PROFILE,
    EVIDENCE_PROFILES,
    normalize_evidence_profile,
)
from .inputs import CompilerInputMixin, MaterializedSource, RenderedSource
''',
          )
          replace_once(
              init,
              '''    "COMPILER_MANIFEST_FORMAT",
    "MAX_BUILD_MANIFEST_BYTES",
''',
              '''    "COMPILER_MANIFEST_FORMAT",
    "DEFAULT_EVIDENCE_PROFILE",
    "EVIDENCE_PROFILES",
    "MAX_BUILD_MANIFEST_BYTES",
''',
          )
          replace_once(
              init,
              '''    "collect_build_diagnostics",
    "read_bounded_bytes",
''',
              '''    "collect_build_diagnostics",
    "normalize_evidence_profile",
    "read_bounded_bytes",
''',
          )

          limits = root / "src/weave_frontend/compiler/limits.py"
          replace_once(limits, 'BUILD_KEY_FORMAT = "weave-build-key-v5"', 'BUILD_KEY_FORMAT = "weave-build-key-v6"')

          capabilities = root / "src/weave_frontend/compiler/capabilities.py"
          text = capabilities.read_text(encoding="utf-8")
          start = text.index("    def require(\n")
          end = text.index("    def form(\n", start)
          require_method = '''    def require(
        self,
        *,
        command: str | None = None,
        protocols: Sequence[str] = (),
        target: str | None = None,
    ) -> dict[str, Any]:
        """Require advertised public capabilities before invoking the compiler."""

        document = self.load()
        commands = {
            item["name"]: item
            for item in document["commands"]
            if isinstance(item, Mapping)
        }
        command_item: Mapping[str, Any] | None = None
        if command is not None:
            command_item = commands.get(command)
            if command_item is None or command_item.get("status") not in {
                "stable",
                "experimental",
            }:
                raise _invalid(
                    "WEAVEC_CAPABILITY_MISSING",
                    f"installed weavec does not advertise command {command!r}",
                )
        protocol_map = {
            item["id"]: item["version"]
            for item in document["protocols"]
            if isinstance(item, Mapping)
        }
        command_protocols = (
            set(command_item.get("protocols", ())) if command_item is not None else None
        )
        for protocol in protocols:
            if protocol not in protocol_map:
                raise _invalid(
                    "WEAVEC_PROTOCOL_UNSUPPORTED",
                    f"installed weavec does not advertise protocol {protocol!r}",
                )
            if command_protocols is not None and protocol not in command_protocols:
                raise _invalid(
                    "WEAVEC_PROTOCOL_UNSUPPORTED",
                    f"weavec command {command!r} does not advertise protocol {protocol!r}",
                )
        if target is not None:
            installed = {
                item["triple"]
                for item in document["targets"]["installed"]
                if isinstance(item, Mapping)
            }
            if target != "native" and target not in installed:
                raise _invalid(
                    "WEAVEC_TARGET_UNSUPPORTED",
                    f"installed weavec does not advertise target {target!r}",
                )
        return document

'''
          capabilities.write_text(text[:start] + require_method + text[end:], encoding="utf-8")

          targets = root / "src/weave_frontend/build_targets.py"
          replace_once(
              targets,
              'from .errors import NotFoundError, ValidationError\n',
              'from .compiler import DEFAULT_EVIDENCE_PROFILE, normalize_evidence_profile\nfrom .errors import NotFoundError, ValidationError\n',
          )
          replace_once(
              targets,
              '''        compiler_target: str | None = None,
        author: str = "agent",
''',
              '''        compiler_target: str | None = None,
        evidence_profile: str | None = None,
        author: str = "agent",
''',
          )
          replace_once(
              targets,
              '''        effective_target = self._normalize_compiler_target(compiler_target)
        state = self.workspace._state(project, branch)
''',
              '''        effective_target = self._normalize_compiler_target(compiler_target)
        effective_profile = normalize_evidence_profile(evidence_profile)
        state = self.workspace._state(project, branch)
''',
          )
          replace_once(
              targets,
              '''            documents[1:],
            effective_target,
            existing=state.get(storage_document),
''',
              '''            documents[1:],
            effective_target,
            effective_profile,
            existing=state.get(storage_document),
''',
          )
          replace_once(
              targets,
              '''            documents[1:],
            effective_target,
        )
''',
              '''            documents[1:],
            effective_target,
            effective_profile,
        )
''',
          )
          replace_once(
              targets,
              '''            target=(
                None
                if config["compiler_target"] == NATIVE_COMPILER_TARGET
                else config["compiler_target"]
            ),
        )
''',
              '''            target=(
                None
                if config["compiler_target"] == NATIVE_COMPILER_TARGET
                else config["compiler_target"]
            ),
            evidence_profile=config["evidence_profile"],
        )
''',
          )
          replace_once(
              targets,
              '''                "additional_documents",
                "compiler_target",
            )
''',
              '''                "additional_documents",
                "compiler_target",
                "evidence_profile",
            )
''',
          )
          replace_once(
              targets,
              '''        additional_documents: list[str],
        compiler_target: str,
    ) -> dict[str, Any]:
''',
              '''        additional_documents: list[str],
        compiler_target: str,
        evidence_profile: str,
    ) -> dict[str, Any]:
''',
          )
          replace_once(
              targets,
              '''            "additional_documents": list(additional_documents),
            "compiler_target": compiler_target,
        }
''',
              '''            "additional_documents": list(additional_documents),
            "compiler_target": compiler_target,
            "evidence_profile": evidence_profile,
        }
''',
          )
          replace_once(
              targets,
              '''        additional_documents: list[str],
        compiler_target: str,
        *,
''',
              '''        additional_documents: list[str],
        compiler_target: str,
        evidence_profile: str,
        *,
''',
          )
          replace_once(
              targets,
              '''        root["children"].append(
            cls._field(
                "compiler-target",
                compiler_target,
                existing=cls._first(existing_fields, "compiler-target"),
            )
        )
        return root
''',
              '''        root["children"].append(
            cls._field(
                "compiler-target",
                compiler_target,
                existing=cls._first(existing_fields, "compiler-target"),
            )
        )
        root["children"].append(
            cls._field(
                "evidence-profile",
                evidence_profile,
                existing=cls._first(existing_fields, "evidence-profile"),
            )
        )
        return root
''',
          )
          replace_once(
              targets,
              '''            if head not in {"primary", "source", "compiler-target"}:
''',
              '''            if head not in {
                "primary",
                "source",
                "compiler-target",
                "evidence-profile",
            }:
''',
          )
          replace_once(
              targets,
              '''        primary = fields["primary"][0]
        sources = fields.get("source", [])
        cls._validate_document_set(primary, sources)
        return cls._config(
            name,
            primary,
            sources,
            fields["compiler-target"][0],
        )
''',
              '''        profiles = fields.get("evidence-profile", [])
        if len(profiles) > 1:
            raise cls._invalid_tree(
                name,
                "at most one evidence-profile field is allowed",
            )
        primary = fields["primary"][0]
        sources = fields.get("source", [])
        cls._validate_document_set(primary, sources)
        return cls._config(
            name,
            primary,
            sources,
            fields["compiler-target"][0],
            normalize_evidence_profile(profiles[0] if profiles else None),
        )
''',
          )

          bridge = root / "src/weave_frontend/compiler/bridge.py"
          replace_once(
              bridge,
              'from .artifacts import CompilerArtifactMixin\n',
              'from .artifacts import CompilerArtifactMixin\nfrom .capabilities import WeavecCapabilities\n',
          )
          replace_once(
              bridge,
              '''from .diagnostics import collect_build_diagnostics
from .inputs import CompilerInputMixin, RenderedSource
''',
              '''from .diagnostics import collect_build_diagnostics
from .evidence import (
    DEFAULT_EVIDENCE_PROFILE,
    normalize_evidence_profile,
    required_evidence_protocols,
)
from .inputs import CompilerInputMixin, RenderedSource
''',
          )
          replace_once(
              bridge,
              '''        build_root: str | Path | None = None,
        timeout_seconds: int = 120,
''',
              '''        build_root: str | Path | None = None,
        capabilities: WeavecCapabilities | None = None,
        timeout_seconds: int = 120,
''',
          )
          replace_once(
              bridge,
              '''        self._configured_compiler = compiler
        self._compiler: Path | None = None
''',
              '''        self._configured_compiler = compiler
        self._compiler: Path | None = None
        workspace_capabilities = getattr(workspace, "capabilities", None)
        self._capabilities = capabilities or (
            workspace_capabilities
            if isinstance(workspace_capabilities, WeavecCapabilities)
            else None
        )
''',
          )
          replace_once(
              bridge,
              '''        revision_id: str | None = None,
        target: str | None = None,
    ) -> dict[str, Any]:
''',
              '''        revision_id: str | None = None,
        target: str | None = None,
        evidence_profile: str | None = None,
    ) -> dict[str, Any]:
''',
          )
          replace_once(
              bridge,
              '''        documents = self._ordered_documents(document, additional_documents)
''',
              '''        profile = normalize_evidence_profile(evidence_profile)
        documents = self._ordered_documents(document, additional_documents)
''',
          )
          replace_once(
              bridge,
              '''        compiler = self._compiler_path()
        compiler_hash = self._sha256_file(compiler)
        cache_payload = {
''',
              '''        compiler = self._compiler_path()
        compiler_hash = self._sha256_file(compiler)
        self._require_evidence_profile(
            compiler,
            compiler_hash=compiler_hash,
            evidence_profile=profile,
            target=target,
        )
        cache_payload = {
''',
          )
          replace_once(
              bridge,
              '''            "compiler_output_limit_bytes": self.max_output_bytes,
            "target": target or "native",
''',
              '''            "compiler_output_limit_bytes": self.max_output_bytes,
            "target": target or "native",
            "evidence_profile": profile,
''',
          )
          replace_once(
              bridge,
              '''                target=target,
                build_id=build_id,
''',
              '''                target=target,
                evidence_profile=profile,
                build_id=build_id,
''',
          )
          replace_once(
              bridge,
              '''        compiler_hash: str,
        target: str | None,
        build_id: str,
''',
              '''        compiler_hash: str,
        target: str | None,
        evidence_profile: str,
        build_id: str,
''',
          )
          replace_once(
              bridge,
              '''            "target": target or "native",
            "compiler_target": (
''',
              '''            "target": target or "native",
            "evidence_profile": evidence_profile,
            "compiler_target": (
''',
          )
          replace_once(
              bridge,
              '''    def _run_compiler(self, command: list[str]) -> BoundedProcessResult:
''',
              '''    def _require_evidence_profile(
        self,
        compiler: Path,
        *,
        compiler_hash: str,
        evidence_profile: str,
        target: str | None,
    ) -> None:
        if evidence_profile == DEFAULT_EVIDENCE_PROFILE:
            return
        capabilities = self._capabilities
        if capabilities is None:
            capabilities = WeavecCapabilities(
                compiler,
                environment_fallback=False,
            )
            self._capabilities = capabilities
        document = capabilities.require(
            command="build",
            protocols=required_evidence_protocols(evidence_profile),
            target=target,
        )
        identity = document.get("_jacquard_identity")
        if not isinstance(identity, dict) or identity.get("compiler_sha256") != compiler_hash:
            raise ValidationError(
                "WEAVEC_CAPABILITY_COMPILER_MISMATCH",
                "compiler capability registry does not identify the selected build binary",
            )

    def _run_compiler(self, command: list[str]) -> BoundedProcessResult:
''',
          )
          replace_once(
              bridge,
              '''        target = manifest.get("target")
        documents = manifest.get("documents")
''',
              '''        target = manifest.get("target")
        evidence_profile = normalize_evidence_profile(manifest.get("evidence_profile"))
        documents = manifest.get("documents")
''',
          )
          replace_once(
              bridge,
              '''            "compiler_output_limit_bytes": manifest["compiler_output_limit_bytes"],
            "target": target,
        }
''',
              '''            "compiler_output_limit_bytes": manifest["compiler_output_limit_bytes"],
            "target": target,
            "evidence_profile": evidence_profile,
        }
''',
          )

          architecture = root / "tests/test_compiler_package_architecture.py"
          replace_once(
              architecture,
              '''    "diagnostics.py",
    "inputs.py",
''',
              '''    "diagnostics.py",
    "evidence.py",
    "inputs.py",
''',
          )
          replace_once(
              architecture,
              '''        "collect_build_diagnostics",
        "validate_compiler_manifest",
''',
              '''        "collect_build_diagnostics",
        "normalize_evidence_profile",
        "validate_compiler_manifest",
''',
          )

          target_tests = root / "tests/test_build_targets.py"
          replace_once(
              target_tests,
              '''        revision_id: str | None = None,
        target: str | None = None,
    ) -> dict[str, Any]:
''',
              '''        revision_id: str | None = None,
        target: str | None = None,
        evidence_profile: str | None = None,
    ) -> dict[str, Any]:
''',
          )
          replace_once(
              target_tests,
              '''            "revision_id": revision_id,
            "target": target,
        }
''',
              '''            "revision_id": revision_id,
            "target": target,
            "evidence_profile": evidence_profile,
        }
''',
          )
          replace_once(
              target_tests,
              '''    assert old["compiler_target"] == "native"
    assert current["additional_documents"] == ["library.weave"]
''',
              '''    assert old["compiler_target"] == "native"
    assert old["evidence_profile"] == "none"
    assert current["additional_documents"] == ["library.weave"]
    assert current["evidence_profile"] == "none"
''',
          )
          replace_once(
              target_tests,
              '''            compiler_target="x86_64-unknown-linux-musl",
        )
''',
              '''            compiler_target="x86_64-unknown-linux-musl",
            evidence_profile="full",
        )
''',
          )
          replace_once(
              target_tests,
              '''            "target": "x86_64-unknown-linux-musl",
        }
''',
              '''            "target": "x86_64-unknown-linux-musl",
            "evidence_profile": "full",
        }
''',
          )
          replace_once(
              target_tests,
              '''        "compiler_target": "x86_64-unknown-linux-musl",
    }
''',
              '''        "compiler_target": "x86_64-unknown-linux-musl",
        "evidence_profile": "full",
    }
''',
          )
          replace_once(
              target_tests,
              '''        with pytest.raises(NotFoundError):
            targets.set("demo", "main", "missing", "missing.weave")
''',
              '''        with pytest.raises(ValidationError) as invalid_profile:
            targets.set(
                "demo",
                "main",
                "bad-profile",
                "main.weave",
                evidence_profile="everything",
            )
        assert invalid_profile.value.code == "INVALID_EVIDENCE_PROFILE"
        with pytest.raises(NotFoundError):
            targets.set("demo", "main", "missing", "missing.weave")
''',
          )

          capability_tests = root / "tests/test_compiler_capabilities.py"
          capability_tests.write_text(
              capability_tests.read_text(encoding="utf-8")
              + '''


def test_requested_protocol_must_belong_to_selected_command(tmp_path: Path) -> None:
    compiler = tmp_path / "weavec"
    registry = _registry()
    commands = registry["commands"]
    assert isinstance(commands, list)
    build = next(item for item in commands if item["name"] == "build")
    build["protocols"] = [
        value
        for value in build["protocols"]
        if value != "weavec-compilation-trace-v1"
    ]
    _write_compiler(compiler, registry)
    capabilities = WeavecCapabilities(compiler, environment_fallback=False)

    with pytest.raises(ValidationError) as captured:
        capabilities.require(
            command="build",
            protocols=("weavec-compilation-trace-v1",),
        )

    assert captured.value.code == "WEAVEC_PROTOCOL_UNSUPPORTED"
''',
              encoding="utf-8",
          )

          profile_tests = root / "tests/test_compiler_evidence_profiles.py"
          profile_tests.write_text(
              '''from __future__ import annotations

import hashlib
from pathlib import Path
from typing import Any

import pytest

from weave_frontend.compiler import CompilerBridge, normalize_evidence_profile
from weave_frontend.errors import ValidationError
from weave_frontend.sexpr_service import SExpressionWorkspace

PROGRAM = """(program
  (name "demo")
  (version "0.1")
  (entry main
    (params)
    (returns i32)
    (do (return (const_i32 42)))))
"""


class RecordingCapabilities:
    def __init__(self, compiler_sha256: str, *, fail: bool = False) -> None:
        self.compiler_sha256 = compiler_sha256
        self.fail = fail
        self.calls: list[dict[str, Any]] = []

    def require(
        self,
        *,
        command: str | None = None,
        protocols: tuple[str, ...] = (),
        target: str | None = None,
    ) -> dict[str, Any]:
        self.calls.append(
            {
                "command": command,
                "protocols": protocols,
                "target": target,
            }
        )
        if self.fail:
            raise ValidationError(
                "WEAVEC_PROTOCOL_UNSUPPORTED",
                "requested compiler evidence is unavailable",
            )
        return {
            "_jacquard_identity": {
                "compiler_sha256": self.compiler_sha256,
            }
        }


class IdentityBridge(CompilerBridge):
    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)
        self.executions = 0

    def _execute_build(self, **kwargs: Any) -> dict[str, Any]:
        self.executions += 1
        return {
            "build_id": kwargs["build_id"],
            "evidence_profile": kwargs["evidence_profile"],
        }


def _compiler(path: Path) -> tuple[Path, str]:
    path.write_text("#!/bin/sh\\nexit 0\\n", encoding="utf-8")
    path.chmod(0o755)
    digest = hashlib.sha256(path.read_bytes()).hexdigest()
    return path, digest


def _workspace(tmp_path: Path, compiler: Path) -> SExpressionWorkspace:
    workspace = SExpressionWorkspace(tmp_path / "weave.db", weavec_binary=compiler)
    workspace.initialize("demo")
    workspace.import_program("demo", "main", "main.weave", PROGRAM)
    return workspace


def test_evidence_profile_validation_is_strict() -> None:
    assert normalize_evidence_profile(None) == "none"
    assert normalize_evidence_profile("minimal") == "minimal"
    assert normalize_evidence_profile("full") == "full"

    with pytest.raises(ValidationError) as captured:
        normalize_evidence_profile("everything")

    assert captured.value.code == "INVALID_EVIDENCE_PROFILE"


def test_evidence_profile_changes_build_identity(tmp_path: Path) -> None:
    compiler, compiler_sha256 = _compiler(tmp_path / "weavec")
    capabilities = RecordingCapabilities(compiler_sha256)
    with _workspace(tmp_path, compiler) as workspace:
        bridge = IdentityBridge(
            workspace,
            compiler=compiler,
            build_root=tmp_path / "builds",
            capabilities=capabilities,  # type: ignore[arg-type]
        )
        none = bridge.build("demo", "main.weave")
        minimal = bridge.build(
            "demo",
            "main.weave",
            evidence_profile="minimal",
        )
        full = bridge.build(
            "demo",
            "main.weave",
            evidence_profile="full",
        )

    assert len({none["build_id"], minimal["build_id"], full["build_id"]}) == 3
    assert [item["evidence_profile"] for item in (none, minimal, full)] == [
        "none",
        "minimal",
        "full",
    ]
    assert [call["command"] for call in capabilities.calls] == ["build", "build"]
    assert all(
        "weavec-compilation-trace-v1" in call["protocols"]
        for call in capabilities.calls
    )


def test_capability_failure_prevents_build_execution(tmp_path: Path) -> None:
    compiler, compiler_sha256 = _compiler(tmp_path / "weavec")
    capabilities = RecordingCapabilities(compiler_sha256, fail=True)
    with _workspace(tmp_path, compiler) as workspace:
        bridge = IdentityBridge(
            workspace,
            compiler=compiler,
            build_root=tmp_path / "builds",
            capabilities=capabilities,  # type: ignore[arg-type]
        )
        with pytest.raises(ValidationError) as captured:
            bridge.build(
                "demo",
                "main.weave",
                evidence_profile="minimal",
            )

    assert captured.value.code == "WEAVEC_PROTOCOL_UNSUPPORTED"
    assert bridge.executions == 0


def test_capability_registry_must_match_selected_compiler(tmp_path: Path) -> None:
    compiler, _ = _compiler(tmp_path / "weavec")
    capabilities = RecordingCapabilities("0" * 64)
    with _workspace(tmp_path, compiler) as workspace:
        bridge = IdentityBridge(
            workspace,
            compiler=compiler,
            build_root=tmp_path / "builds",
            capabilities=capabilities,  # type: ignore[arg-type]
        )
        with pytest.raises(ValidationError) as captured:
            bridge.build(
                "demo",
                "main.weave",
                evidence_profile="full",
            )

    assert captured.value.code == "WEAVEC_CAPABILITY_COMPILER_MISMATCH"
    assert bridge.executions == 0
''',
              encoding="utf-8",
          )

          docs = root / "docs/compiler-package.md"
          replace_once(
              docs,
              '''- `diagnostics.py`: compiler diagnostic validation and node mapping;
- `manifest.py`: build-manifest validation;
''',
              '''- `diagnostics.py`: compiler diagnostic validation and node mapping;
- `evidence.py`: strict evidence profiles and their capability requirements;
- `manifest.py`: build-manifest validation;
''',
          )

          workflow = root / ".github/workflows/evidence-profile-generator.yml"
          workflow.unlink()
          PY

      - name: Validate generated tree
        run: |
          python -m pip install -e '.[dev]'
          python -m ruff format .
          python -m ruff check --fix .
          python -m compileall -q src tests scripts
          python -m pytest -q \
            tests/test_build_targets.py \
            tests/test_compiler_capabilities.py \
            tests/test_compiler_evidence_profiles.py \
            tests/test_compiler_package_architecture.py
          python -m ruff check .
          git diff --check

      - name: Commit generated implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit \
            -m "feat(compiler): add evidence profiles" \
            -m "Add strict none, minimal, and full evidence profiles to revisioned build targets, compiler capability preflight, and build-key identity."
          git push origin HEAD:agent/evidence-profile-generator
